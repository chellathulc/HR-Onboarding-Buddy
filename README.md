# HR-Onboarding-Buddy

A React application for HR onboarding processes.

## Getting Started

1. Install dependencies: `npm install`

2. Run the development server: `npm run dev`

3. Build for production: `npm run build`

## Project Structure

- `src/` - Source code
- `public/` - Static assets
- `dist/` - Build output